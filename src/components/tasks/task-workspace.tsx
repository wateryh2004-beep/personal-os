"use client";

import { useMemo, useState } from "react";
import { CheckCircle2, RefreshCw, RotateCcw, Sparkles } from "lucide-react";
import { Dialog, DialogContent } from "@/components/ui/dialog";
import { MicrosoftTodoCreateDialog } from "@/components/tasks/microsoft-todo-create-dialog";
import { TaskAssistant } from "@/components/tasks/task-assistant";
import { completeMicrosoftTodoTaskAction, reopenMicrosoftTodoTaskAction, syncAndBackupMicrosoftTodoAction, syncMicrosoftTodoAction } from "@/features/tasks/microsoft-todo";

type TodoList = { id: string; display_name: string; is_default: boolean };
type TodoTask = { id: string; title: string; body_text: string | null; status: string; due_at: string | null; completed_at: string | null; todo_list_id: string; importance: string };
type Filter = "all" | "priority" | "due";

function TaskRow({ task, listName }: { task: TodoTask; listName: string }) {
  return <article className="group grid grid-cols-[auto_minmax(0,1fr)_auto] items-start gap-3 border-b px-3 py-3 last:border-b-0 hover:bg-[#F7F7F5]"><form action={completeMicrosoftTodoTaskAction}><input type="hidden" name="task_id" value={task.id} /><button aria-label={`完成 ${task.title}`} className="mt-0.5 text-zinc-400 transition-colors hover:text-[#365F78] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#365F78]"><CheckCircle2 size={19} /></button></form><div className="min-w-0"><h2 className="truncate text-sm font-medium text-zinc-900">{task.title || "无标题任务"}</h2>{task.body_text ? <p className="mt-1 line-clamp-1 text-xs text-zinc-500">{task.body_text}</p> : null}</div><div className="flex items-center gap-2 text-xs text-zinc-400"><span className={task.importance === "high" ? "text-[#365F78]" : ""}>{task.importance === "high" ? "高优先级" : listName}</span>{task.due_at ? <time className="font-mono">{new Date(task.due_at).toLocaleDateString("zh-CN", { month: "numeric", day: "numeric" })}</time> : null}</div></article>;
}

export function TaskWorkspace({ lists, tasks }: { lists: TodoList[]; tasks: TodoTask[] }) {
  const [filter, setFilter] = useState<Filter>("all");
  const [assistantOpen, setAssistantOpen] = useState(false);
  const listNames = useMemo(() => new Map(lists.map((list) => [list.id, list.display_name])), [lists]);
  const active = tasks.filter((task) => task.status !== "completed");
  const completed = tasks.filter((task) => task.status === "completed");
  const visible = active.filter((task) => filter === "all" || (filter === "priority" && task.importance === "high") || (filter === "due" && Boolean(task.due_at)));
  const filterLabel: Record<Filter, string> = { all: "全部", priority: "高优先", due: "有截止日期" };
  return <section className="flex h-[calc(100dvh-118px)] min-h-[500px] flex-col overflow-hidden border bg-white"><header className="flex flex-wrap items-center justify-between gap-3 border-b px-4 py-2"><div className="flex items-center gap-1"><span className="mr-2 text-sm font-medium">待办</span>{(["all", "priority", "due"] as Filter[]).map((item) => <button key={item} type="button" onClick={() => setFilter(item)} className={`border px-2 py-1 text-xs ${filter === item ? "border-[#365F78] bg-[#EDF3F6] text-[#365F78]" : "border-transparent text-zinc-500 hover:border-zinc-200"}`}>{filterLabel[item]}</button>)}</div><div className="flex flex-wrap items-center gap-2"><button type="button" onClick={() => setAssistantOpen(true)} className="inline-flex items-center gap-1 border px-2 py-1 text-xs text-[#365F78]"><Sparkles size={14} />AI</button><form action={syncMicrosoftTodoAction}><button className="inline-flex items-center gap-1 border px-2 py-1 text-xs text-[#365F78]"><RefreshCw size={14} />刷新</button></form><form action={syncAndBackupMicrosoftTodoAction}><button className="border px-2 py-1 text-xs text-[#365F78]">对齐</button></form><MicrosoftTodoCreateDialog lists={lists} /></div></header><div className="min-h-0 flex-1 overflow-y-auto"><div className="grid min-h-full lg:grid-cols-[minmax(0,1fr)_280px]"><main className="min-w-0"><div className="flex items-center justify-between border-b px-4 py-3"><p className="text-xs text-zinc-500">{filterLabel[filter]} · {visible.length}</p><p className="text-xs text-zinc-400">完成后自动同步</p></div>{visible.length ? <div>{visible.map((task) => <TaskRow key={task.id} task={task} listName={listNames.get(task.todo_list_id) || "任务"} />)}</div> : <div className="px-4 py-14 text-center"><p className="text-sm font-medium">没有符合条件的待办</p><p className="mt-2 text-xs text-zinc-500">可以新建任务，或切换筛选查看其他任务。</p></div>}</main><aside className="border-l bg-[#F7F7F5] p-4"><p className="text-xs font-medium tracking-wide text-zinc-500">清单</p><div className="mt-3 space-y-1">{lists.map((list) => <div key={list.id} className="flex items-center justify-between py-1.5 text-sm"><span className="truncate">{list.display_name}</span><span className="font-mono text-xs text-zinc-400">{active.filter((task) => task.todo_list_id === list.id).length}</span></div>)}</div>{completed.length ? <details className="mt-7 border-t pt-4"><summary className="cursor-pointer text-sm text-zinc-500">已完成 {completed.length}</summary><div className="mt-3 space-y-2">{completed.slice(0, 12).map((task) => <div key={task.id} className="flex items-center justify-between gap-2 text-xs"><span className="min-w-0 truncate text-zinc-400 line-through">{task.title || "无标题任务"}</span><form action={reopenMicrosoftTodoTaskAction}><input type="hidden" name="task_id" value={task.id} /><button aria-label={`恢复 ${task.title || "无标题任务"}`} className="shrink-0 text-[#365F78] hover:underline"><RotateCcw size={13} /></button></form></div>)}</div></details> : null}</aside></div></div><Dialog open={assistantOpen} onOpenChange={setAssistantOpen}><DialogContent className="max-h-[82dvh] overflow-y-auto bg-white p-6 sm:max-w-2xl"><TaskAssistant lists={lists} /></DialogContent></Dialog></section>;
}
