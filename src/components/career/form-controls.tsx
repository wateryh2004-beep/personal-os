export function Field({ label, name, defaultValue, type = "text", required = false, placeholder }: { label: string; name: string; defaultValue?: string | null; type?: string; required?: boolean; placeholder?: string }) { return <label className="grid gap-1 text-sm"><span>{label}</span><input name={name} type={type} required={required} defaultValue={defaultValue ?? ""} placeholder={placeholder} className="border bg-white px-3 py-2" /></label>; }
export function TextField({ label, name, defaultValue, placeholder }: { label: string; name: string; defaultValue?: string | null; placeholder?: string }) { return <label className="grid gap-1 text-sm"><span>{label}</span><textarea name={name} defaultValue={defaultValue ?? ""} placeholder={placeholder} className="min-h-24 border bg-white px-3 py-2" /></label>; }
type SelectOption = string | { value: string; label: string };
export function SelectField({ label, name, values, defaultValue }: { label: string; name: string; values: readonly SelectOption[]; defaultValue?: string }) { return <label className="grid gap-1 text-sm"><span>{label}</span><select name={name} defaultValue={defaultValue} className="border bg-white px-3 py-2">{values.map((item) => { const value = typeof item === "string" ? item : item.value; const text = typeof item === "string" ? item : item.label; return <option key={value} value={value}>{text}</option>; })}</select></label>; }
export const PrimaryButton = ({ children }: { children: React.ReactNode }) => <button className="w-fit bg-[#365F78] px-3 py-2 text-sm text-white hover:bg-[#294b60]">{children}</button>;
