"use client";

import { useMemo, useState, useTransition } from "react";
import { Bot, ChevronLeft, ChevronRight, Filter, Plus, RefreshCw, Settings2 } from "lucide-react";
import { useRouter } from "next/navigation";
import { CalendarAssistant } from "@/components/calendar/calendar-assistant";
import { CalendarCreateForm } from "@/components/calendar/calendar-create-form";
import { CalendarEventEditForm } from "@/components/calendar/calendar-event-edit-form";
import { CalendarFullView } from "@/components/calendar/calendar-full-view";
import type { CalendarEventRecord } from "@/features/calendar/types";
import { CalendarCategoryManager } from "@/components/calendar/calendar-category-manager";
import { AISidecar } from "@/components/ai/ai-sidecar";
import { Dialog, DialogContent } from "@/components/ui/dialog";
import { syncAndBackupMicrosoftAction, updateCalendarEvent } from "@/features/calendar/actions";
import type { CalendarCategory } from "@/features/calendar/categories/types";
import { useWorkspacePanel } from "@/components/layout/workspace-panel-provider";
import { Inspector } from "@/components/shared/inspector";

type View = "day" | "week" | "month";
type Draft = { startsAt: string; endsAt: string };

export function CalendarWorkspace({ events, categories, timezone, scopeReady, initialCreateOpen = false }: { events: CalendarEventRecord[]; categories: CalendarCategory[]; timezone: string; scopeReady: boolean; initialCreateOpen?: boolean }) {
  const router = useRouter();
  const [view, setView] = useState<View>("week");
  const [cursor, setCursor] = useState(() => new Date());
  const [selected, setSelected] = useState<CalendarEventRecord | null>(null);
  const [draft, setDraft] = useState<Draft | null>(() => { if (!initialCreateOpen) return null; const now = new Date(); return { startsAt: now.toISOString(), endsAt: new Date(now.getTime() + 3_600_000).toISOString() }; });
  const [filterOpen, setFilterOpen] = useState(false); const [settingsOpen, setSettingsOpen] = useState(false); const [selectedCategories, setSelectedCategories] = useState<Set<string>>(new Set()); const [syncing, startSync] = useTransition();
  const ai = useWorkspacePanel("calendar-ai"); const inspector = useWorkspacePanel("calendar-inspector");
  const filtered = useMemo(() => selectedCategories.size ? events.filter((event) => event.categories.some((category) => selectedCategories.has(category))) : events, [events, selectedCategories]);
  const openEvent = (event: CalendarEventRecord) => { setSelected(event); setDraft(null); inspector.open(); };
  const openDraft = (range: Draft) => { setSelected(null); setDraft(range); inspector.open(); };
  const sync = () => startSync(async () => { await syncAndBackupMicrosoftAction(); router.refresh(); });
  const changeCursor = (amount: number) => setCursor((date) => new Date(date.getFullYear(), view === "month" ? date.getMonth() + amount : date.getMonth(), view === "day" ? date.getDate() + amount : view === "week" ? date.getDate() + amount * 7 : date.getDate()));
  const moveEvent = async (event: CalendarEventRecord, range: Draft & { isAllDay: boolean }) => { const form = new FormData(); form.set("provider_event_id", event.provider_event_id); form.set("original_subject", event.subject); form.set("original_starts_at", event.starts_at); form.set("original_ends_at", event.ends_at); form.set("subject", event.subject); form.set("description", event.body_text ?? ""); form.set("location_name", event.location_name ?? ""); form.set("starts_at", range.startsAt); form.set("ends_at", range.endsAt); form.set("is_all_day_present", "true"); if (range.isAllDay) form.set("is_all_day", "on"); form.set("importance", event.importance); form.set("show_as", event.show_as === "unknown" ? "busy" : event.show_as); form.set("classification_mode", "auto"); form.set("preserve_categories", "true"); const result = await updateCalendarEvent({ status: "idle", message: "" }, form); if (result.status !== "success") throw new Error(result.message); router.refresh(); };
  return <section className="flex h-[calc(var(--app-viewport-height)-var(--toolbar-height))] min-h-0 overflow-hidden bg-white"><div className="flex min-w-0 flex-1 flex-col"><header className="relative flex min-h-14 shrink-0 items-center justify-between gap-2 border-b px-3"><div className="flex items-center gap-1"><button onClick={() => changeCursor(-1)} aria-label="上一段日期"><ChevronLeft size={17} /></button><button onClick={() => setCursor(new Date())} className="px-2 text-sm">今天</button><button onClick={() => changeCursor(1)} aria-label="下一段日期"><ChevronRight size={17} /></button><span className="ml-2 text-sm font-medium">{cursor.toLocaleDateString("zh-CN", { month: "long", day: view === "month" ? undefined : "numeric", year: view === "month" ? "numeric" : undefined })}</span></div><div className="flex items-center gap-1"><div className="rounded bg-[var(--surface-hover)] p-0.5">{(["day", "week", "month"] as View[]).map((item) => <button key={item} onClick={() => setView(item)} className={`rounded px-2 py-1 text-xs ${view === item ? "bg-white text-[var(--accent)]" : ""}`}>{item === "day" ? "日" : item === "week" ? "周" : "月"}</button>)}</div><button onClick={sync} disabled={syncing} aria-label="对齐 Outlook" className="p-2 text-[var(--accent)]"><RefreshCw size={15} className={syncing ? "animate-spin" : ""} /></button><button onClick={() => setFilterOpen(!filterOpen)} aria-label="筛选分类" className="p-2"><Filter size={15} /></button><button onClick={() => setSettingsOpen(true)} aria-label="分类设置" className="p-2"><Settings2 size={15} /></button><button onClick={ai.toggle} aria-label="Calendar AI" className="p-2 text-[var(--accent)]"><Bot size={15} /></button><button onClick={() => { const now = new Date(); openDraft({ startsAt: now.toISOString(), endsAt: new Date(now.getTime() + 3_600_000).toISOString() }); }} className="rounded bg-[var(--accent)] px-3 py-1.5 text-xs text-white"><Plus size={14} className="mr-1 inline" />新建</button></div>{filterOpen ? <div className="absolute right-24 top-12 z-30 w-56 rounded border bg-white p-2 shadow"><button onClick={() => setSelectedCategories(new Set())} className="mb-2 text-xs text-[var(--accent)]">清除筛选</button>{categories.map((category) => <label key={category.id} className="flex gap-2 py-1 text-xs"><input type="checkbox" checked={selectedCategories.has(category.display_name)} onChange={() => setSelectedCategories((current) => { const next = new Set(current); if (next.has(category.display_name)) next.delete(category.display_name); else next.add(category.display_name); return next; })} />{category.display_name}</label>)}</div> : null}</header><CalendarFullView key={`${view}:${cursor.toDateString()}`} events={filtered} timezone={timezone} initialView={view === "day" ? "timeGridDay" : view === "week" ? "timeGridWeek" : "dayGridMonth"} initialDate={cursor} onOpen={openEvent} onCreate={openDraft} onMove={moveEvent} /></div><Inspector open={inspector.isOpen} title={selected ? "日程详情" : "新建日程"} onClose={inspector.close} className="w-[min(380px,calc(100vw-8px))]">{selected ? <CalendarEventEditForm event={selected} timezone={timezone} calendarCategories={categories} categoriesEnabled={scopeReady} /> : draft ? <CalendarCreateForm timezone={timezone} categoriesEnabled={scopeReady} initialStart={draft.startsAt} initialEnd={draft.endsAt} /> : null}</Inspector><AISidecar open={ai.isOpen} onClose={ai.close} context="Calendar"><CalendarAssistant timezone={timezone} categories={categories} /></AISidecar><Dialog open={settingsOpen} onOpenChange={setSettingsOpen}><DialogContent><CalendarCategoryManager categories={categories} scopeReady={scopeReady} events={events} referenceTime={cursor.getTime()} /></DialogContent></Dialog></section>;
}
