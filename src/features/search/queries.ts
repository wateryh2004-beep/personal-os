import { z } from "zod"; import { requireOwner } from "@/lib/auth/require-owner"; import { searchInputSchema, type GlobalSearchResult } from "./types";
const row=z.object({domain:z.enum(["notes","career","files","tasks","calendar","reviews"]),entity_type:z.string(),entity_id:z.string().uuid(),title:z.string(),subtitle:z.string(),snippet:z.string(),metadata:z.record(z.string(),z.unknown()),source_updated_at:z.string().nullable(),score:z.number()});
export async function searchPersonalOs(input: z.input<typeof searchInputSchema>):Promise<GlobalSearchResult[]> { const parsed=searchInputSchema.parse(input); if(Array.from(parsed.query).length<2)return []; const {supabase}=await requireOwner(); const {data,error}=await supabase.rpc("search_personal_os",{p_query:parsed.query,p_limit:parsed.limit,p_domains:parsed.domains??null}); if(error) throw new Error("search_unavailable"); return z.array(row).parse(data??[]).map((item)=>({id:`${item.entity_type}:${item.entity_id}`,domain:item.domain,entityType:item.entity_type,entityId:item.entity_id,title:item.title||"未命名",subtitle:item.subtitle||null,snippet:item.snippet||null,href:item.entity_type==="note"?`/notes/${item.entity_id}`:item.entity_type==="experience"?`/career/experiences/${item.entity_id}`:item.domain==="files"?"/files":item.domain==="tasks"?"/tasks":item.domain==="calendar"?"/calendar":item.domain==="reviews"?"/reviews":"/career",score:item.score,sourceUpdatedAt:item.source_updated_at,metadata:item.metadata})); }
