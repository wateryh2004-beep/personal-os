export type NowCalendarEvent = { id: string; subject: string; starts_at: string; ends_at: string; is_all_day: boolean; location_name: string | null };
export type NowTask = { id: string; title: string; due_at: string | null; importance: string | null; status: string };
export type NowCareerMilestone = { id: string; track_id: string; career_direction_id: string | null; title: string; starts_on: string | null; target_date: string; status: string; importance: string | null };
export type NowAvailability = "ready" | "unavailable";
export type NowAttentionItem = { id: string; kind: "overdue_tasks" | "career_milestone" | "inbox" | "calendar_sync"; priority: "critical" | "high" | "medium" | "low"; title: string; description?: string; href: string };
export type NowNextAction = { kind: "event"; event: NowCalendarEvent; state: "ongoing" | "starting_soon" | "upcoming"; reason: string; href: "/calendar" } | { kind: "task"; task: NowTask; reason: string; href: "/tasks" } | { kind: "career_milestone"; milestone: NowCareerMilestone; reason: string; href: "/career/roadmap" } | { kind: "inbox"; count: number; reason: string; href: "/inbox" } | { kind: "none"; reason: string };
export type NowUpcomingItem = { id: string; kind: "event" | "task" | "milestone"; title: string; at: string; href: string; detail?: string };
export type NowWorkspace = { timezone: string; calendar: { today: NowCalendarEvent[]; upcoming: NowCalendarEvent[]; connection: { status: string; last_sync_at: string | null; last_error_code: string | null } | null }; tasks: { overdue: NowTask[]; today: NowTask[]; upcoming: NowTask[] }; career: { upcomingMilestones: NowCareerMilestone[] }; inboxCount: number; nextAction: NowNextAction; attention: NowAttentionItem[]; upcoming: NowUpcomingItem[]; availability: { calendar: NowAvailability; tasks: NowAvailability; career: NowAvailability; inbox: NowAvailability }; summary: { todayEventCount: number; todayTaskCount: number; attentionCount: number } };
