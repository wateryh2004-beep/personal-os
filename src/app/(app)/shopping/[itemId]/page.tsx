import { notFound } from "next/navigation";import { ShoppingItemDetail } from "@/components/shopping/shopping-item-detail";import { getPurchaseItem } from "@/features/shopping/queries";export default async function ShoppingItemPage({params}:{params:Promise<{itemId:string}>}){const {itemId}=await params;const {item,unavailable}=await getPurchaseItem(itemId);if(unavailable)return <p>Shopping 数据暂时无法读取。</p>;if(!item)notFound();return <ShoppingItemDetail item={item}/>;}
