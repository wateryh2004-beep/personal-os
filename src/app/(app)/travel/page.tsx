import { TravelWorkspace } from "@/components/travel/travel-workspace";import { getTrips } from "@/features/travel/queries";export default async function TravelPage({searchParams}:{searchParams:Promise<{create?:string}>}){const [{trips,unavailable},params]=await Promise.all([getTrips(),searchParams]);if(unavailable)return <p>Travel 数据暂时无法读取。</p>;return <TravelWorkspace trips={trips} initialCreateOpen={params.create==="1"}/>;}
