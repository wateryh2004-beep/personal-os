import Link from "next/link";
import { createFolder, createNote } from "@/features/notes/actions";
import { getNotesWorkspace } from "@/features/notes/queries";
import { PageHeader } from "@/components/shared/page-header";
export default async function Notes() { const { notes, folders } = await getNotesWorkspace(); return <><PageHeader title="Notes" description="原始 Markdown 是正文的唯一权威格式。" action={<form action={createNote}><button className="bg-[#365F78] px-3 py-2 text-sm text-white">新建笔记</button></form>} /><div className="grid gap-7 lg:grid-cols-[270px_1fr]"><aside className="border-r pr-5"><form action={createFolder} className="flex gap-2"><input name="name" required placeholder="新建文件夹" className="min-w-0 border p-2 text-sm" /><input type="hidden" name="parent_id" value="" /><button className="border px-2 text-sm">添加</button></form><h2 className="mt-6 text-xs text-zinc-500">文件夹</h2><div className="mt-2 space-y-1 text-sm">{folders.map((folder) => <p key={folder.id}>{folder.name}</p>)}{!folders.length ? <p className="text-zinc-500">尚无文件夹</p> : null}</div><h2 className="mt-6 text-xs text-zinc-500">笔记</h2><div className="mt-2 divide-y">{notes.map((note) => <Link className="block py-3 text-sm hover:text-[#365F78]" href={`/notes/${note.id}`} key={note.id}>{note.title || "无标题笔记"}<span className="mt-1 block font-mono text-xs text-zinc-400">{new Date(note.updated_at).toLocaleDateString("zh-CN")}</span></Link>)}</div></aside><section className="grid place-items-center border bg-white p-8 text-center"><div><h2 className="text-lg font-medium">{notes.length ? "选择一篇笔记继续编辑" : "还没有笔记。"}</h2><p className="mt-2 text-sm text-zinc-500">创建第一篇笔记，开始建立你的个人知识库。</p></div></section></div></>; }
